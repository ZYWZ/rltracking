/* Proto definition for initial tracking results. */

syntax = "proto3";

import "common.proto";

message Tracklets {
    repeated Tracklet tracklet = 1;
    DetectionType detection_type = 2;
    Sequences sequence = 3;
}